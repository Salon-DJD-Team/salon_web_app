name: "salondjd-webapp"


services:
  salondjd-webapp:
    build:
      context: .
      dockerfile: ./Dockerfile
      target: production-build
    ports:
      - "80:80"
    environment:
      API_URL: "http://localhost:8080/api"

